meta {
  name: login
  type: http
  seq: 2
}

post {
  url: http://{{host}}:{{port}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "passoword": "admin",
    "code": "{{captchaCode}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
