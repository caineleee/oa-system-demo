meta {
  name: captchat
  type: http
  seq: 1
}

get {
  url: http://{{host}}:{{port}}/captcha?env=test
  body: none
  auth: inherit
}

params:query {
  env: test
}

vars:post-response {
  captcha_code: {{response.headers['captcha']}}
}

settings {
  encodeUrl: true
  timeout: 0
}
